{
  "name": "@skycoin4444/crypto-envelope",
  "version": "1.1.0",
  "description": "Small AES-256-GCM envelope utility with authenticated decryption validation",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "tsc --noEmit -p tsconfig.json",
    "audit": "npm audit --audit-level=high",
    "verify": "npm run lint && npm test && npm run build && npm run audit"
  },
  "keywords": ["skycoin4444", "aes-gcm", "encryption"],
  "author": "SKYCOIN4444",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.3",
    "vitest": "^3.2.0"
  }
}
